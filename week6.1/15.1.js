// Linked List - List where multiple elements will be linked

class Person {
    constructor(leg, arm) {
        this.leg = leg;
        this.arm = arm;
    }
    // properties
    // leg = 2;
    // arm = 2;

    // method
    walk() {
        console.log('is walking......');
    }
}

const samim = new Person(2, 2);
const specialPerson = new Person(3, 2);
// console.log(samim.walk());
console.log(specialPerson);
